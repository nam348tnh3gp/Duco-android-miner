name: ducominer
description: Duino-Coin Miner for Android
publish_to: 'none'
version: 1.0.0+10

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  ffi: ^2.1.0
  shared_preferences: ^2.2.2
  flutter_foreground_task: ^5.0.0 
  wakelock_plus: ^1.1.0     

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icon/duco.png